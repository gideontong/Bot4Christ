class Reminder {
    /**
     * Creates a reminder that automatically fires
     * @param {str} type : what custom command do you want to run?
     * @param {TextChannel} channel : a channel to send the reminder in
     * @param {int} interval : how long between runs
     * @param {Date} start : time you want the reminder to first run
     */
    static createReminder(type, channel, interval, start) {
    }

    static genericReminder(content) {
    }

    static templatePandemic(content) {
    }

    static templateDailyVerse(content) {
    }
}

module.exports = Reminder;